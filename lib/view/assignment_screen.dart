import 'package:flutter/material.dart';

class AssignmetScreen extends StatelessWidget {
  const AssignmetScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}